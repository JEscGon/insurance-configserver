spring:
  application:
    name: insurance_configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JEscGon/insurance-configserver
          default-label: main
          search-paths:
            - config
          clone-on-start: true
        compatibility-verifier:
          enabled: false
server:
  port: 8888
